// import { useMutation } from 'react-query';
// import instance from '../metacon-axios';
// import { metacon } from '../config';

export function useMetaCertificate() {
  // const { mutateAsync, isLoading, isError } = useMutation(
  //   (encodedCert, password) => {
  //     const decodedCertificate = encodedCert.toString('base64');
  //     // atob(thing)
  //     console.log('decodedCertificate', decodedCertificate);
  //   },
  // );
}
